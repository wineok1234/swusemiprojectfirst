<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.swusemiprojectfirst">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Fragment_4"></activity>
        <activity android:name=".Fragment_3" />
        <activity android:name=".NewActivity" />
        <activity android:name=".MyItem" />
        <activity android:name=".Fragment_2" />
        <activity android:name=".Fragment_1" />
        <activity android:name=".TabActivity" />
        <activity android:name=".RegActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>